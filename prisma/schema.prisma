// Prisma schema file
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // Change this to your database provider (postgresql, mysql, sqlite, etc.)
  url      = env("DATABASE_URL")
}